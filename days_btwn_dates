class Solution:
    def daysBetweenDates(self, date1: str, date2: str) -> int:
        date_one = date.fromisoformat(date1)
        date_two  = date.fromisoformat(date2)

        res = abs(date_one -  date_two)
        
        return res.days
